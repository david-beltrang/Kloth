<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema base de la app: claro, sin ActionBar (la UI usa Compose). -->
    <style name="Theme.Kloth" parent="android:Theme.Material.Light.NoActionBar" />
</resources>